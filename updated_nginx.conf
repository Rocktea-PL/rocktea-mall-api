# Rate limiting zones for testing
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=20r/m;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=100r/m;

# Dev API
server {
    server_name api-dev.yourockteamall.com;
    client_max_body_size 100M;

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location /static/ {
        root /home/ubuntu/django-app/dev/main;
    }

    # Health check endpoint (no rate limiting)
    location /health/ {
        access_log off;
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    # Auth endpoints with relaxed rate limiting for testing
    location ~ ^/(rocktea/signup/user|store/login|rocktea/storeowner|mall/signin|api/accounts/admin/login)/ {
        limit_req zone=auth_limit burst=10 nodelay;
        limit_req_status 429;
        
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    location / {
        limit_req zone=general_limit burst=20 nodelay;
        limit_req_status 429;
        
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/api-dev.yourockteamall.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api-dev.yourockteamall.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = api-dev.yourockteamall.com) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name api-dev.yourockteamall.com;
    return 404;
}

# Production API
server {
    server_name api.yourockteamall.com;
    client_max_body_size 100M;

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location /static/ {
        root /home/ubuntu/django-app/prod/main;
    }

    # Health check endpoint (no rate limiting)
    location /health/ {
        access_log off;
        include proxy_params;
        proxy_pass http://127.0.0.1:8001;
    }

    # Auth endpoints with relaxed rate limiting for testing
    location ~ ^/(rocktea/signup/user|store/login|rocktea/storeowner|mall/signin|api/accounts/admin/login)/ {
        limit_req zone=auth_limit burst=10 nodelay;
        limit_req_status 429;
        
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8001;
    }

    location / {
        limit_req zone=general_limit burst=20 nodelay;
        limit_req_status 429;
        
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8001;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/api.yourockteamall.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.yourockteamall.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = api.yourockteamall.com) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name api.yourockteamall.com;
    return 404;
}

# Monitoring Dev
server {
    listen 80;
    server_name monitor-dev.yourockteamall.com;

    location / {
        proxy_pass http://127.0.0.1:5555;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' ws: wss:; frame-ancestors 'self';" always;
    }
}

# Monitoring Prod
server {
    listen 80;
    server_name monitor.yourockteamall.com;

    location / {
        proxy_pass http://127.0.0.1:5556;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' ws: wss:; frame-ancestors 'self';" always;
    }
}













































# Strict approach on auth endpoint
ubuntu@ip-172-31-39-61:~$ sudo cat /etc/nginx/sites-available/api-dev.yourockteamall.com
# Dev API
server {
    server_name api-dev.yourockteamall.com;
    client_max_body_size 100M;

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location /static/ {
        root /home/ubuntu/django-app/dev/main;
    }

    # Health check endpoint (no rate limiting)
    location /health/ {
        access_log off;
        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    location / {
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8000;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/api-dev.yourockteamall.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api-dev.yourockteamall.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = api-dev.yourockteamall.com) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name api-dev.yourockteamall.com;
    return 404;
}

# Production API
server {
    server_name api.yourockteamall.com;
    client_max_body_size 100M;

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location /static/ {
        root /home/ubuntu/django-app/prod/main;
    }

    # Health check endpoint (no rate limiting)
    location /health/ {
        access_log off;
        include proxy_params;
        proxy_pass http://127.0.0.1:8001;
    }

    location / {
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'self';" always;

        include proxy_params;
        proxy_pass http://127.0.0.1:8001;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/api.yourockteamall.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.yourockteamall.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = api.yourockteamall.com) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name api.yourockteamall.com;
    return 404;
}

# Monitoring Dev
server {
    listen 80;
    server_name monitor-dev.yourockteamall.com;

    location / {
        proxy_pass http://127.0.0.1:5555;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' ws: wss:; frame-ancestors 'self';" always;
    }
}

# Monitoring Prod
server {
    listen 80;
    server_name monitor.yourockteamall.com;

    location / {
        proxy_pass http://127.0.0.1:5556;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' ws: wss:; frame-ancestors 'self';" always;
    }
}